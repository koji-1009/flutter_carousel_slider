name: carousel_slider_x
description: A carousel slider widget, support infinite scroll and auto play, enlarge mode.
repository: https://github.com/koji-1009/flutter_carousel_slider
version: 5.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
